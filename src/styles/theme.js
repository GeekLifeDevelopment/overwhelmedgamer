const colors = {
  owBlack: "#222222",
  owOrange: "#f3b33e",
};

const breakpoints = {
  tablet: "768px",
  mobileLarge: "425px",
};

const fonts = {
  regular: `'Source Code Pro', monospace`,
  heading: `'Kanit', sans-serif`,
};

const theme = {
  colors,
  breakpoints,
  fonts,
};

export default theme;
